package com.dofus.network.model;

/**
 * Direction of network traffic.
 */
public enum Direction {
    /**
     * Server → Client (inbound to client).
     */
    INBOUND,

    /**
     * Client → Server (outbound from client).
     */
    OUTBOUND
}
